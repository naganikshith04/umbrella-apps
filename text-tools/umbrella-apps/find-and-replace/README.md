# Find and Replace

Search and replace text with support for regex, case sensitivity, and whole word matching

## Features

- Simple text find and replace
- Regular expression (regex) support
- Case-sensitive and whole-word options
- Replace all or one-by-one confirmation
- Highlight all matches before replacing

## Keywords

find and replace, text replace, search and replace, regex replace, bulk replace, text substitution, find text, replace words

## Deployment

This app is ready to deploy to Vercel:

```bash
vercel deploy
```

Or simply push to GitHub and connect to Vercel for automatic deployments.

## Local Development

Open `index.html` in a web browser or use a local server:

```bash
python -m http.server 8000
```

Then visit http://localhost:8000

---

Generated by AI App Generator
